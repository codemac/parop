;;; This is a test program, to see if I can do Parenthesis Orient
;;; Programming.
;;;
;;; This is obviously influenced almost entirely by iMatix's gsl. I'm
;;; still not sure I know what XNF is though.

~(display "#include <stdio.h>\n")
~(display "int main(int argc, char **argv) {\n")
~(display "	printf(\"Hello World\");\n")
~(display "}\n")

~(for-each (lambda _
#~
This should be printed for each
~#)
	   '(1 2 3 4))

